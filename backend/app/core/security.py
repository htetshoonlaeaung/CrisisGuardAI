# backend/app/core/security.py
# CORS middleware configuration and rate limiting setup.
# Controls which origins may access the FastAPI backend.

# TODO: Configure CORSMiddleware with settings.CORS_ORIGINS
# TODO: Add slowapi rate limiter to prevent abuse on emergency endpoints
