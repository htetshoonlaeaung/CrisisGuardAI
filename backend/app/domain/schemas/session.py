# backend/app/domain/schemas/session.py
# Pydantic v2 schemas for emergency session create and retrieve responses.

from pydantic import BaseModel
from typing import Optional

# TODO: Define CreateSessionRequest (domain)
# TODO: Define SessionResponse (session_token, domain, current_severity, is_active, created_at)
