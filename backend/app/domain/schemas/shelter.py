# backend/app/domain/schemas/shelter.py
# Pydantic v2 schemas for emergency shelter query requests and responses.

from pydantic import BaseModel
from typing import Optional

# TODO: Define ShelterQueryRequest (lat, lon, radius_km, disaster_type)
# TODO: Define ShelterResponse (id, name, disaster_type, latitude, longitude, capacity, current_occupancy, contact_phone, is_open)
